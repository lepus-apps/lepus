import {
  "moonbitlang/core/json",
  "justjavac/ffi",
}

supported_targets = "+native"

options(
  link: { "native": { "cc-link-flags": "-Llib -lwebview" } },
  "native-stub": [ "stub.c" ],
  targets: {
    "binding.mbt": [ "native" ],
    "command.mbt": [ "native" ],
    "plugin.mbt": [ "native" ],
    "webview.mbt": [ "native" ],
  },
)

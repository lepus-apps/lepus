import {
  "moonbitlang/core/env",
  "moonbitlang/core/encoding/utf8",
  "moonbitlang/x/sys",
  "moonbitlang/async",
  "moonbitlang/async/process",
  "moonbitlang/async/pipe",
}

supported_targets = "+native"

pkgtype(kind: "executable")

options(
  "bin-name": "cc",
  "bin-target": "native",
  "native-stub": [ "target_stub.c" ],
)

import {
  "moonbitlang/core/json",
  "lepus-apps/webview",
  "moonbitlang/core/encoding/utf8" @utf8,
}

supported_targets = "+native"

options(
  "native-stub": [ "biometric_native.c" ],
  targets: { "biometric.mbt": [ "native" ], "plugin.mbt": [ "native" ] },
)

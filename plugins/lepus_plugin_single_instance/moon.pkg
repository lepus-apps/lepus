import {
  "moonbitlang/core/json",
  "lepus-apps/lepus/webview",
  "moonbitlang/core/encoding/utf8",
}

supported_targets = "+native"

options(
  "native-stub": [ "single_instance_native.c" ],
  targets: { "plugin.mbt": [ "native" ], "single_instance.mbt": [ "native" ] },
)

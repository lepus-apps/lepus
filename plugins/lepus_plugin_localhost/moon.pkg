import {
  "moonbitlang/core/json",
  "lepus-apps/webview",
  "moonbitlang/core/encoding/utf8" @utf8,
}

supported_targets = "+native"

options(
  "native-stub": [ "localhost_native.c" ],
  targets: { "localhost.mbt": [ "native" ], "plugin.mbt": [ "native" ] },
)
